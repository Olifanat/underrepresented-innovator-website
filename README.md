# Instructions:

```
git clone https://github.com/Olifanat/underrepresented-innovator-website.git 
make changes locally
git add <filename>
git commit -m "the thing i changed"
git push
```
to pull the most recent changes locally:

```
git pull
```
